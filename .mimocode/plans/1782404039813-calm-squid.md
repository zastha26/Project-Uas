# Plan: Recreate Bank Sampah Code into Project-Uas + nabila.txt

## Context
Kode bank sampah pernah dibuat di sesi 24 Juni di path `C:\Users\nabil\Proyek-UAS\my-app\my-app` (folder sudah tidak ada). Kode lengkap sudah di-recover dari history. Sekarang perlu di-copy ke project saat ini.

## Yang Dikerjakan
1. Buat `app/layout.tsx` — update metadata Bank Sampah
2. Buat `app/globals.css` — tambah custom theme hijau
3. Buat `app/page.tsx` — homepage lengkap (Navbar, Hero, Stats, Tentang Kami, Fitur, CTA, Footer)
4. Buat `app/auth/page.tsx` — login & register dengan tab switcher
5. Tampilkan semua kode ke `nabila.txt` di root project

## Files
| File | Aksi |
|------|------|
| `app/layout.tsx` | Rewrite (update metadata) |
| `app/globals.css` | Rewrite (tambah custom theme) |
| `app/page.tsx` | Rewrite (homepage lengkap) |
| `app/auth/page.tsx` | Baru (login/register) |
| `nabila.txt` | Baru (semua kode dikumpulkan) |

## Verification
1. `pnpm run dev` → buka localhost:3000, cek homepage
2. Klik "Masuk" → navigate ke /auth, test tab switch Login/Register
3. Buka nabila.txt → pasti semua kode lengkap
